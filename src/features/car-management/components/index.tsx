import React from 'react';
import { Box, Typography } from '@/shared/components';

const CarManagement: React.FC = () => {
  return (
    <Box>
      <Typography variant="h1">Car Management</Typography>
    </Box>
  );
};

export default CarManagement;
