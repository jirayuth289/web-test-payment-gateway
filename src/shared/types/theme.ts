/**
 * Re-export MUI theme types for consistent imports across the application
 * 
 * These types are re-exported from @mui/material/styles to provide:
 * - Consistent import paths
 * - Centralized type definitions
 * - Easier maintenance if we need to switch UI libraries in the future
 */

export type { Theme, ThemeOptions } from '@mui/material/styles';
export type { CSSObject } from '@mui/material/styles';

